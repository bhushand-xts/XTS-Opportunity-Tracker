{
  "name": "xts-backend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "graphql": "16.10.0"
  },
  "scripts": {
    "install:all": "npm install && cd gateway && npm install && cd ../services/user && npm install && cd ../account && npm install && cd ../opportunity && npm install && cd ../estimation && npm install && cd ../approval && npm install && cd ../rfp && npm install && cd ../document && npm install && cd ../notification && npm install && cd ../reporting-audit && npm install && cd ../admin && npm install"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
