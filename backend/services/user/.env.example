# Copy to .env and fill in. .env is git-ignored — never commit real values.

PORT=4001

DB_HOST=localhost
DB_PORT=5432
DB_NAME=xts_user
DB_USER=
DB_PASSWORD=
DB_POOL_MAX=10

# the /admin dashboard proxies to the admin service
ADMIN_SERVICE_URL=http://localhost:4010/graphql
