# Copy to .env and fill in. .env is git-ignored — never commit real values.

PORT=4010

DB_HOST=localhost
DB_PORT=5432
DB_NAME=xts_admin
DB_USER=
DB_PASSWORD=
DB_POOL_MAX=10

# admin checks role usage through user's GraphQL API (separate database)
USER_SERVICE_URL=http://localhost:4001/graphql
