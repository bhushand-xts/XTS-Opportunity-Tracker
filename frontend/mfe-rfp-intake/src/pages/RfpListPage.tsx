import { Button, StageBadge } from "@xts/design-system";

export function RfpListPage() {
  return (
    <section>
      <h1>RFP Intake & Response</h1>
      <StageBadge stage="identified" />
      <p>List view for this domain -- replace with the real query.</p>
      <Button>New</Button>
    </section>
  );
}
